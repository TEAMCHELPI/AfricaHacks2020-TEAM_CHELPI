/**----------State----------**/
export const state = () => ( {
  authenticated: true,
} );

/**----------Getters----------**/
export const getters = {
  authenticated(state) {
    return state.authenticated;
  }
};

/**----------Getters----------**/
export const mutations = {}

/**----------Actions----------**/
export const actions = {}
